import React from 'react'
import Button from '@mui/material/Button';

export default function tagButton() {
  return (
    <Button variant='contained' sx={{ borderRadius: 8 }}>tagButton</Button>
  )
}
